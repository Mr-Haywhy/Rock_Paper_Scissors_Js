# Rock_Paper_Scissors_Js

# The game LOGIC

- Create an array to store the choices
- Create the function to get computer's choice
- Create another function to get User's choice
- Create two variables to hold the players score.

* Inside the computer choice write a code to get the computer's choice at random, so that it returns "Rock", "Paper" or "Scissirs".
  - Use math.random to select at random.
